# Default values for graphql-meter

replicaCount: 1

image:
  repository: ghcr.io/vanditsramblings/graphql-meter
  tag: "latest"
  pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

service:
  type: ClusterIP
  port: 8899

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # cert-manager.io/cluster-issuer: letsencrypt
  hosts:
    - host: graphql-meter.local
      paths:
        - path: /
          pathType: Prefix
  tls: []
  #  - secretName: graphql-meter-tls
  #    hosts:
  #      - graphql-meter.local

# Environment variables passed to the container
env:
  JWT_SECRET: "change-me-in-production"
  MAX_CONCURRENT_RUNS: "3"
  ENABLE_K6: "true"
  ENABLE_LOCUST: "true"

# Persistent volume for SQLite data and run artifacts
persistence:
  enabled: true
  storageClass: ""
  accessModes:
    - ReadWriteOnce
  size: 2Gi

resources:
  requests:
    cpu: 250m
    memory: 256Mi
  limits:
    cpu: "2"
    memory: 1Gi

nodeSelector: {}
tolerations: []
affinity: {}
